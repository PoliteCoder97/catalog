<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.politecoder.catalog">

  <uses-permission android:name="android.permission.INTERNET"/>
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
  <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

  <application
    android:name="project.classes.App"
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:largeHeap="true"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="false"
    android:theme="@style/AppTheme"
    android:usesCleartextTraffic="true">
    <activity android:name="project.person.PersonContactActivity">
    </activity>
    <activity android:name="project.view.SplashActivity">
    </activity>
    <activity android:name="project.category.CategoryListActivity">
    </activity>
    <activity android:name="project.comment.CommentListActivity">
    </activity>
    <activity android:name="project.person.PersonListActivity">
    </activity>
    <activity android:name="project.product.ProductActivity">
    </activity>
    <activity android:name="project.product.ProductListActivity">
    </activity>
    <activity android:name="project.view.MainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>
  </application>

</manifest>